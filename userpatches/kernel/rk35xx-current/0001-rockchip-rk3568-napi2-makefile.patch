diff --git a/arch/arm64/boot/dts/rockchip/Makefile b/arch/arm64/boot/dts/rockchip/Makefile
index 38aaab5b2661..b38747d53b1f 100644
--- a/arch/arm64/boot/dts/rockchip/Makefile
+++ b/arch/arm64/boot/dts/rockchip/Makefile
@@ -455,7 +455,20 @@ dtb-$(CONFIG_ARCH_ROCKCHIP) += rk3588s-tablet-v10.dtb
 dtb-$(CONFIG_ARCH_ROCKCHIP) += rk3588s-tablet-v11.dtb
 dtb-$(CONFIG_ARCH_ROCKCHIP) += rk3566-luckfox-core3566.dtb
 dtb-$(CONFIG_ARCH_ROCKCHIP) += rk3566-roc-pc.dtb
+dtb-$(CONFIG_ARCH_ROCKCHIP) += rk3568-napi2.dtb
 
 subdir-y := $(dts-dirs) overlay
 
 # Armbian: Incremental: assuming overlay targets are already in the Makefile
+
+# Armbian: Incremental: assuming overlay targets are already in the Makefile
+
+# Armbian: Incremental: assuming overlay targets are already in the Makefile
+
+# Armbian: Incremental: assuming overlay targets are already in the Makefile
+
+# Armbian: Incremental: assuming overlay targets are already in the Makefile
+
+# Armbian: Incremental: assuming overlay targets are already in the Makefile
+
+# Armbian: Incremental: assuming overlay targets are already in the Makefile
\ No newline at end of file
